var app = require('./server-config.js');

var port = 4568;

app.listen(port);

console.log('this is the most badass console log you have ever seen - pkovan');
console.log('Server now listening on port ' + port);

